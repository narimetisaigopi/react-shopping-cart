export function handleLinkClick(event: React.MouseEvent<HTMLAnchorElement>) {
    event.preventDefault(); // Prevents the browser from following the link
    // Perform your JavaScript action here, such as opening a modal
  }